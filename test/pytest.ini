[pytest]
filterwarnings =
    ignore:Using the in-memory storage for tracking rate limits.*
    ignore:the load_module\(\) method is deprecated.*
    ignore:The Query.get\(\) method is considered legacy.*
